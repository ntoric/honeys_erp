# Server configuration
PORT=8080

# Feature toggles
# Set to 'true' to allow GORM to purge conflicting soft-deleted records and reuse their mobile number details.
# Set to 'false' to block and throw a 'A soft-deleted party with this mobile number already exists' validation error.
ALLOW_SOFT_DELETED_REUSE=true
